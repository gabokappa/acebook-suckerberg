<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @album.name %>
</p>

<%= form_for @album do |f| %>
<div class="field">
   <%= f.label :pic %><br/>
 <%= f.file_field :pic %>
</div>
<%= f.submit "Submit" %>
<% end %>


<%= link_to 'Edit', edit_album_path(@album) %> |
<%= link_to 'Back', albums_path %>
